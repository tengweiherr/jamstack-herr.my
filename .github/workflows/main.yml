name: Deploy to Vercel
run-name: Deploy to ${{ inputs.deploy_target }} by @${{ github.actor }}
on:
  release:
    types: [published]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - run: curl -X POST -d {} https://api.vercel.com/v1/integrations/deploy/sample_deploy_hook
